# SPDX-FileCopyrightText: 2026 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : monado
version     : "25.1.0+git.983a8a51"
release     : 3
homepage    : https://monado.dev/
upstreams   :
    - git|https://gitlab.freedesktop.org/monado/monado.git : 983a8a518356041fba6445d3548792e2ef0d6948
summary     : The open source XR runtime
description : |
    Monado is an open source XR runtime delivering immersive experiences such as VR and AR on mobile, PC/desktop,
    and any other device (because gosh darn people come up with a lot of weird hardware).
    Monado aims to be a complete and conforming implementation of the OpenXR API made by Khronos.
    The project is primarily developed on GNU/Linux, but also runs on Android and Windows.
    "Monado" has no specific meaning and is just a name.
license     : BSL-1.0
builddeps   :
    - binary(doxygen)
    - binary(glslangValidator)
    - pkgconfig(bluez)
    - pkgconfig(dbus-1)
    - pkgconfig(egl)
    - pkgconfig(eigen3)
    - pkgconfig(gl)
    - pkgconfig(gstreamer-1.0)
    - pkgconfig(gstreamer-app-1.0)
    - pkgconfig(gstreamer-video-1.0)
    - pkgconfig(hidapi-libusb)
    - pkgconfig(libbsd)
    - pkgconfig(libcjson)
    - pkgconfig(libdrm)
    - pkgconfig(libjpeg)
    - pkgconfig(libusb-1.0)
    - pkgconfig(opencv4)
    - pkgconfig(python3)
    - pkgconfig(sdl2)
    - pkgconfig(udev)
    - pkgconfig(vulkan)
    - pkgconfig(wayland-client)
    - pkgconfig(wayland-protocols)
    - pkgconfig(wayland-scanner)
    - pkgconfig(x11)
    - pkgconfig(x11-xcb)
    - pkgconfig(xcb)
    - pkgconfig(xrandr)
    - pkgconfig(zlib)
    # - OpenHMD
    # - realsense2
    # - rerun_sdk
    # - depthai
    # - LeapV2
    # - LeapSDK
    # - ONNXRuntime
    # - Percetto
    # - libuvc
    # - survive
    # - OpenVR
rundeps     :
    - xr-hardware
setup       : |
    %cmake
build       : |
    %cmake_build
install     : |
    %cmake_install
check       : |
    %cmake_test
paths       :
    - /usr/include/monado/monado.h
    - /usr/lib/libmonado.so
    - /usr/lib/libopenxr_monado.so

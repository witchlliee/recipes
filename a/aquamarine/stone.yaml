# SPDX-FileCopyrightText: 2025 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : aquamarine
version     : "0.14.0"
release     : 7
upstreams   :
    - https://github.com/hyprwm/aquamarine/archive/refs/tags/v0.14.0.tar.gz : 5dcf0b17f7dd51539fd7e79d68484f04240b3b63cf9f5f21d5b6dea0088168f9
homepage    : https://github.com/hyprwm/aquamarine
license     : BSD-3-Clause
summary     : A very light linux rendering backend library
description : |
    Aquamarine is a very light linux rendering backend library. It provides basic abstractions for an application to render on a Wayland session (in a window) or a native DRM session. It is agnostic of the rendering API (Vulkan/OpenGL) and designed to be lightweight, performant, and minimal. Aquamarine provides no bindings for other languages. It is C++-only.
builddeps   :
    - cmake(hyprwayland-scanner)
    - pkgconfig(gbm)
    - pkgconfig(hwdata)
    - pkgconfig(hyprutils)
    - pkgconfig(libdisplay-info)
    - pkgconfig(libdrm)
    - pkgconfig(libinput)
    - pkgconfig(libseat)
    - pkgconfig(libudev)
    - pkgconfig(opengl)
    - pkgconfig(pixman-1)
    - pkgconfig(wayland-client)
    - pkgconfig(wayland-protocols)
setup       : |
    %cmake
build       : |
    %cmake_build
install     : |
    %cmake_install

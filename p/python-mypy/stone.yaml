# SPDX-FileCopyrightText: 2025 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : python-mypy
version     : "2.3.0"
release     : 6
homepage    : https://www.mypy-lang.org/
upstreams   :
    - https://pypi.debian.net/mypy/mypy-2.3.0.tar.gz : 465965d41cd9a2726694e983e8ce7113259327bec798115d1e1dfa2a52fb666e
summary     : Optional static typing for Python 3 (PEP484)
description : |
    Mypy is an optional static type checker for Python that aims to combine the benefits of dynamic (or "duck") typing and static typing. Mypy combines the expressive power and convenience of Python with a powerful type system and compile-time type checking. Mypy type checks standard Python programs; run them using any Python VM with basically no runtime overhead.
license     : MIT
builddeps   :
    - python(ast-serialize)
    - python(librt)
    - python(mypy-extensions)
    - python(pathspec)
    - python(setuptools)
    - python(typing-extensions)
    # Man pages need help2man
rundeps     :
    - binary(python3)
build       : |
    # There are a couple of dependencies specified by the build that are unused and thus unpackaged
    %pyproject_build --skip-dependency-check
install     : |
    %pyproject_install

    # # Man pages
    # %install_dir %(installroot)%(mandir)/man1
    # PYTHONPATH="$PWD"/build/lib

# SPDX-FileCopyrightText: 2025 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : pybind11
version     : "3.0.4"
release     : 4
homepage    : https://pybind11.readthedocs.org/
upstreams   :
    - https://github.com/pybind/pybind11/archive/refs/tags/v3.0.4.tar.gz : 74b6a2c2b4573a400cafb6ecbf60c98df300cd3d0041296b913d02b2cbbb2676
summary     : A lightweight header-only library that exposes C++ types in Python and vice versa
description : |
    A lightweight header-only library that exposes C++ types in Python and vice versa
license     : BSD-3-Clause
builddeps   :
    - cmake(Boost)
    - pkgconfig(python3)
    - python(scikit-build-core)
rundeps     :
    - python-libs
checkdeps   :
    - python(pytest)
setup       : |
    %cmake
build       : |
    %pyproject_build
    %cmake_build
install     : |
    %pyproject_install
    %cmake_install
check       : |
    %cmake_build --target check
packages    :
    - "%(name)":
        provides-exclude:
            # We want these to be provided by the devel package
            - 'pkgconfig\(pybind11\)'
            - 'cmake\(pybind11\)'

    - "%(name)-devel":
        paths:
            - /usr/bin/pybind11-config
            - /usr/share/cmake/

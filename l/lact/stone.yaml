# SPDX-FileCopyrightText: 2026 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : lact
version     : "0.9.1"
release     : 3
upstreams   :
    - https://github.com/ilya-zlobintsev/LACT/archive/refs/tags/v0.9.1.tar.gz : cb6790ffb99104ffb93502c82e19b9e8d07ec7c1eadf131948694d48eba49789
homepage    : https://github.com/ilya-zlobintsev/LACT
license     : MIT
summary     : Linux GPU Configuration Tool
description : |
    Linux GPU Configuration Tool
networking  : true
builddeps   :
    - binary(cargo)
    - binary(vulkaninfo)
    - pkgconfig(OpenCL)
    - pkgconfig(gobject-2.0)
    - pkgconfig(graphene-gobject-1.0)
    - pkgconfig(libadwaita-1)
    - pkgconfig(libdisplay-info)
    - pkgconfig(libdrm)
rundeps     :
    - binary(vulkaninfo)
    - hwdata
setup       : |
    %patch %(pkgdir)/0001-Adapt-to-our-amdgpu-kernel-argument.patch
    %patch %(pkgdir)/863.patch
build       : |
    %make build-release
install     : |
    %make_install PREFIX=%(prefix)
    %install_file %(pkgdir)/kernel-lact.cmdline %(installroot)%(libdir)/kernel/cmdline.d/70-lact.cmdline

    # Enable system-wide upon install
    %install_file %(pkgdir)/98-lact.preset -t %(installroot)/%(libdir)/systemd/system-preset

/** Human readable report. This is not consumed by boulder */
{
	"manifest-version": "0.2",
	"packages": {
		"build-essential": {
			"depends": [
				"binary(bc)",
				"binary(bison)",
				"binary(boulder)",
				"binary(bunzip2)",
				"binary(clang)",
				"binary(clang++)",
				"binary(cpio)",
				"binary(diff)",
				"binary(ent)",
				"binary(fj)",
				"binary(flex)",
				"binary(g++)",
				"binary(gcc)",
				"binary(gh)",
				"binary(git)",
				"binary(just)",
				"binary(ld.bfd)",
				"binary(ld.lld)",
				"binary(make)",
				"binary(patch)",
				"binary(pkgconf)",
				"binary(ps)",
				"binary(python)",
				"binary(renice)",
				"binary(tar)",
				"binary(unxz)",
				"binary(unzip)",
				"glibc-devel",
				"layout",
				"linux-headers",
				"pkgconfig(libelf)",
				"pkgconfig(libssl)",
				"pkgconfig(libxcrypt)",
				"pkgconfig(ncurses)",
				"python(ruamel-yaml)"
			],
			"files": [
				"/usr/share/doc/build-essential/README"
			],
			"name": "build-essential"
		}
	},
	"source-name": "build-essential",
	"source-release": "10",
	"source-version": "0"
}

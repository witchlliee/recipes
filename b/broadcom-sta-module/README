This package uses patches directly from Debian and as such differs slightly from other extramod packages in order to make those apply cleanly.

To update the patches clone https://salsa.debian.org/broadcom-sta-team/broadcom-sta and copy the debian/patches directly without the series file.

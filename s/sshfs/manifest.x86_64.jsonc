/** Human readable report. This is not consumed by boulder */
{
	"manifest-version": "0.2",
	"packages": {
		"sshfs": {
			"build-depends": [
				"binary(rst2man)",
				"pkgconfig(fuse3)",
				"pkgconfig(glib-2.0)"
			],
			"depends": [
				"binary(sftp)",
				"binary(ssh)",
				"interpreter(/usr/lib/ld-linux-x86-64.so.2(x86_64))",
				"soname(libc.so.6(x86_64))",
				"soname(libfuse3.so.4(x86_64))",
				"soname(libglib-2.0.so.0(x86_64))"
			],
			"files": [
				"/usr/bin/sshfs",
				"/usr/sbin/mount.fuse.sshfs",
				"/usr/sbin/mount.sshfs",
				"/usr/share/man/man1/sshfs.1"
			],
			"name": "sshfs",
			"provides": [
				"binary(sshfs)",
				"sysbinary(mount.fuse.sshfs)",
				"sysbinary(mount.sshfs)"
			]
		}
	},
	"source-name": "sshfs",
	"source-release": "3",
	"source-version": "3.7.6"
}

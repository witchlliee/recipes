# SPDX-FileCopyrightText: 2025 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : sshfs
version     : "3.7.6"
release     : 3
upstreams   :
    - https://github.com/libfuse/sshfs/archive/refs/tags/sshfs-3.7.6.tar.gz : 67a3e166a39b07708497ee0aee308547dba386053cf8d816b4ce8a9b3066a6ce
homepage    : https://github.com/libfuse/sshfs
license     : GPL-2.0-only
summary     : A network filesystem client to connect to SSH servers
description : |
    SSHFS allows you to mount a remote filesystem using SFTP. Most SSH servers support and enable this SFTP access by default, so SSHFS is very simple to use - there's nothing to do on the server-side.
builddeps   :
    - binary(rst2man)
    - pkgconfig(fuse3)
    - pkgconfig(glib-2.0)
rundeps     :
    - binary(sftp)
    - binary(ssh)
setup       : |
    %patch %(pkgdir)/man-fuse3.patch

    %meson
build       : |
    %meson_build
install     : |
    %meson_install
